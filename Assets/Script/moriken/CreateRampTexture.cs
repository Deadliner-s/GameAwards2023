using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CreateRampTexture : MonoBehaviour
{
    [SerializeField]
    public Gradient _gradient = default;

    [SerializeField]
    public int width = 0;

    [SerializeField]
    public int heigth = 0;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
